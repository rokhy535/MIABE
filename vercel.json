{
  "version": 2,
  "builds": [
    {
      "src": "artistid-frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "buildCommand": "npm run build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/artistid-frontend/dist/$1"
    }
  ]
}